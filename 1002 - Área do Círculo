#include <stdio.h>
#include <math.h>
 
int main() {
 
        double R, A, N;
    scanf ("%lf", &R);
        N = 3.14159;
        A = N*pow(R,2);
            printf("A=%.4lf",A);
            printf("\n");
    return 0;
}
